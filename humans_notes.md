maybe rename this Deco Assaying


i think we want to dockerize this. that'll mean no more access to local dir paths. so onlt analyse a single file or a repo url. and we need to build out output in out own local file system and privide toosl to get dir listing of it and to download files from it. maybe even to zip up an entire output dir to be downloaded as a single file.

